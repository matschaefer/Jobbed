/**
 * Entwicklungs-Environment. Der Angular-Dev-Server proxied `/api` an das
 * Backend auf Port 8080 (siehe proxy.conf.json).
 */
export const environment = {
  production: false,
  apiBaseUrl: '/api/v1',
};
